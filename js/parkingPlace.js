class parkingPlace {
  constructor(ctx, startPosX, startPosY, width, height) {
    this.ctx = ctx;
    this.width = width;
    this.height = height;
    this.posX = startPosX;
    this.posY = startPosY;
  }
}
